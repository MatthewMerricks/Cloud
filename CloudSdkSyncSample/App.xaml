<Application x:Class="CloudSdkSyncSample.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CloudSdkSyncSample.ViewModels"
             xmlns:views="clr-namespace:CloudSdkSyncSample.Views">
    <Application.Resources>
        <vm:SyncStatusViewModel x:Key="SyncStatusViewModel" />
        <!-- The unused Style below is required because of a bug in the frameworks that refuses to acknowledge a single resource in the Resources collection. -->
        <Style x:Key="unused" />
    </Application.Resources>
</Application>